html
   head
      title 휴먼 토론방
      meta(name='viewport', content='user-scalable=no, initial-scale=1.0,maximum-scale=1.0, minimum-scale=1.0, width=device-width')
      link(rel='stylesheet', href='/chat.css', type='text/css')
      //link(rel='stylesheet', href='/tip-yellow.css', type='text/css')
      script(src="https://code.jquery.com/jquery-3.2.1.js")
      script(src="/socket.io/socket.io.js")
      script(src="/chat.js")
      script(src="/client/chat.js")
      //script(src="/jquery.poshytip.js")
   body
      
      div.header 
        //h1 ㅎㅁ
     
      div.map
      form#chat
        input#name.name(type='text', autocomplete='off' value='var a')
        textarea#message.message(placeholder='Insert', autocomplete='off')
        input.chat(value='return ')
      if(user)
         //h1 Hi, #{user.name}
      else
         //a(href = "/sign_up") Sign Up